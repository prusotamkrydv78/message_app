export const metadata = {
  title: {
    default: "Groups | ChatX",
    template: "%s | ChatX",
  },
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function GroupsLayout({ children }) {
  return children;
}
